{
  "name": "mind-store",
  "private": true,
  "workspaces": ["app", "api"],
  "scripts": {
    "dev:api": "npm run dev -w api",
    "dev:app": "npm run web -w app",
    "build": "npm run build -w app && npm run build -w api",
    "start": "npm run start -w api"
  },
  "engines": { "node": ">=20" }
}
